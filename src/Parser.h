#ifndef PARSER_H_
#define PARSER_H_

#include <vector>
#include <iostream>
#include <fstream>
#include <sstream>
#include <cstdlib>

#include "Shape.h"

using namespace std;

class Parser {
  public:
    vector<Shape*> readFile(string file) {
      vector<Shape*> patch_list;


      return patch_list;
    }
};

#endif
